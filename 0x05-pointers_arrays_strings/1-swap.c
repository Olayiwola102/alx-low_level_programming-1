#include "main.h"
/**
* swap_int - swapping of variables value of the integers
* @a: A pointer to be swapped
* @b: A pointer to be swapped
* Return: void that means the answer is correct
*/

void swap_int(int *a, int *)
{
	*a += *b;
	*b = *a - *b;
	*a = *a - *b;
}
